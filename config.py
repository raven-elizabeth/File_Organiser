downloads = {
    "windows": "Downloads", # Rename if your downloads folder is called something else
    "folders": ["Image", "Video", "Audio", "Document", "Application", "Misc"] # Edit sub-folder names if necessary (may break code)
}

# Could add language choice later